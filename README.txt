SMALL STEPS — FOREST HOME FIX PATCH

Replace/upload ONLY these files into the existing GitHub repository:
- css/base.css
- js/app.js
- sw.js
- manifest.webmanifest

This patch:
- removes the separate Happenings home button
- keeps Happenings as the sealed letter in the header
- removes the decorative Stretching and Meditation icons
- removes the obsolete phone-mockup forest background reference
- uses the full Forest Theme 1 artwork as the actual app background
- ensures the Forest Path logo is the active app/onboarding logo
- bumps the service-worker cache to v1.29

The existing Forest Theme 1 image/icon assets from Beta 1.28 are intentionally NOT included because they do not need replacing.
